@model OutputDayEditModel

<div class="modal-content">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title" id="myModalLabel">Редактирование наименования богослужебного дня</h4>
    </div>
    <div class="modal-body">
        @if (Model == null)
        {
            <div class="row">
                <h4>Произошла ошибка доступа</h4>
            </div>
        }
        else
        {
            <form id="editOutputForm" asp-controller="Schedule" asp-action="EditDay" asp-route-id="@Model.Id">
                @{/*<div class="row">
                    <div class="col-sm-4"><input type="checkbox" asp-for="Name.IsBold" /> <b>Жирный</b></div>
                    <div class="col-sm-4"><input type="checkbox" asp-for="Name.IsItalic" /> <i>Курсивом</i></div>
                    <div class="col-sm-4"><input type="checkbox" asp-for="Name.IsRed" /> Красным</div>
                </div>*/}
                <div class="row">
                    <div class="form-group col-sm-12">
                        <label asp-for="Name">Наименование:</label>
                    </div>
                    <div class="form-group col-sm-12">
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group col-sm-12">
                        <label>Печатный шаблон:</label>
                    </div>
                    <div class="form-group col-sm-12">
                        <select class="form-control" asp-for="PrintTemplateId" asp-items="@ViewBag.PrintTemplates">
                        </select>
                    </div>

                    <div class="col-xs-12"><button id="editDayBtn" class="btn btn-primary">Сохранить</button></div>
                </div>
            </form>
        }

    </div>
</div>
